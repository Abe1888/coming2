import React from 'react';
import ReactDOM from 'react-dom/client';
import ModelTestPage from './ModelTestPage';

ReactDOM.createRoot(document.getElementById('model-test-root')!).render(
  <React.StrictMode>
    <ModelTestPage />
  </React.StrictMode>
);
